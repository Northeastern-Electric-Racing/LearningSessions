# Learning Session: TypeScript

## Setup

1. clone the Repository.
2. cd into this folder
3. run:

#### `yarn install`

Generates your node-modules folder.

#### `yarn start`

Runs the `index.ts` script

## Exercise

Fill out `index.ts`
